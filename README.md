The project is finally at the final runthrough stage 
it 
will 
soon 
be 
submitted 
REFLECTION QUESTIONS:
1. Version control is very useful because it enables us to keep track of the chnages made to our code overtime. This enables corrections to be made to the code and if any changes were done and why it let's people work on the same project without overwriting the work either puts into it. 
2. Staging which is "git add" enables me to say the chnages i want in the next snapshot. The committing "git commit" takes that snapshot and saves it to permanent history, it comfirms it. 
3. I think commiting should be done after completing a logical and somewhat closed chunck of work. It shouldn't be too small or too large and should be done often. 
4. git init turns an existing local folder to an empty git repo. git clone copies an existing remote repo to my machine with the included history
5. Good commit messages matter because they are a good way to keep track of why we or other people made the changes we did by looking at the log. It's important for debugging or collabing. 
6. Purpose branches let us work on different features or experiments in isolation without touching or altering the main "stable" branch. Then we can merge it once we have a good version that we are no longer experimenting on. 
7. We create a new branch any time we want to create a distinct new features fix or an experiment as previously mentionned especially when working on a group project rather than comitting it directly to the main. 
